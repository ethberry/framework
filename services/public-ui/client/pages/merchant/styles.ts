import {createStyles, makeStyles} from "@material-ui/core";

export default makeStyles(
  () =>
    createStyles({
      avatar: {
        width: 200,
        height: 200,
      },
    }),
  {name: "Merchant"},
);
